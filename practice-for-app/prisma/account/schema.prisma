generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./account.db"
}

model Account {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  level     Int      @default(1) // デフォルトを「1」にしておくと便利
}
